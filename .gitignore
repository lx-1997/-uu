node_modules/
dist/
dist-server/
.env
# 本地/发行用 Supabase 内嵌密钥（勿提交）
config/supabase-conversation.embedded.json
release/
# 克隆前端参考仓后，桌面打包会优先使用其中的 icon（见 scripts/desktop-icons.mjs）
rdkstudio_frontend-master/
build-resources/
openclaw-main/

# Build artifacts
*.tsbuildinfo

# Logs & temp
error.log
*.log
temp_*.txt

# 本地 LLM token 统计（已改为仅内存，若旧版生成过可忽略）
data/llm-token-usage.json
# Runtime data (regenerated by server; avoid noisy / machine-specific commits)
data/ecosystem-registry.json
